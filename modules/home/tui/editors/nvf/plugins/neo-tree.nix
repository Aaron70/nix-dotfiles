{
  filetree.neo-tree = { 
    enable = true;

    setupOpts = {
      filesystem = {
        window = {
          position = "right";
          mappings = {
            "\\" = "close_window"; 
          };
        };
      };
    };
  };
}
